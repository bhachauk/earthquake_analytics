{
  "builds": [
    {
      "src": "api/index.go",
      "use": "@vercel/go"
    }
  ],
  "functions": {
    "api/index.go": {
      "runtime": "go1.x",
      "memory": 128,
      "maxDuration": 10
    }
  },
  "routes": [
    { "src": "/(.*)", "dest": "/api/index.go" }
  ]
}